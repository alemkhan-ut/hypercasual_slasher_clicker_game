using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Coin : MonoBehaviour
{
    private Enemy _enemy;

    private void Start()
    {
        //_enemy = transform.parent.transform.parent.GetComponent<Enemy>();

        //for (int i = 0; i < _enemy.obstacles.Length; i++)
        //{
        //    if (gameObject.GetComponent<ObjectInEnemy>().position == _enemy.obstacles[i].GetComponent<ObjectInEnemy>().position)
        //    {
        //        Destroy(gameObject);

        //        break;
        //    }
        //}
    }

}

